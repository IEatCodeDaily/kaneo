# KFL-367 QC review — PASS (cycle 2/3)

Reviewer: `omp/9router/glm/glm-5.3` (same reviewer as cycle 1). Stage doc: `docs/agents-pipeline.md` §S3. Input: S1 spec + actual diff, not the implementer summary.

## Verdict

**PASS.** All five substantive cycle-1 defects are verified fixed by re-running the suites myself (not by trusting the report). Three minor, honestly-declared residual gaps remain; none breaks a contract, none requires another implementation cycle, and one is auto-resolved by S4's mandatory `pnpm build`. Conditions for S4 recorded below.

## Cycle-1 defect verification

### 1. Membership API absent → FIXED

- `apps/api/src/project/index.ts:376-466`: `GET /project/:id/tickets` (`fromProject` + `project:read`), `POST /project/:id/tickets` and `DELETE /project/:id/tickets/:taskId` (`fromProject` + `project:update` + `task:update`).
- Controllers `add/list/remove-project-ticket.ts` exist. Add controller proves same-org inside the insert transaction (`add-project-ticket.ts:72`), checks Board `edit` via `getResourcePrivilege` with non-leaking 404s (lines 57-74), returns 409 on existing membership checked in-transaction (lines 76-85), inserts association metadata only.
- Integration cases 1-4, 7 pass (within the 10/10 below).

### 2. Progress projection absent → FIXED

- `withPresentationOnlyFields` deleted; repo-wide grep hits only pipeline docs, zero source.
- `project-projection.ts`: one set-based `loadMembershipRows` join (project_ticket→task→board, lines 99-122), `filterVisibleRows` intersecting `listAccessibleResourceIds` board grants (129-145), `deriveProjectProgress` excluding `deletedAt`/`archivedAt`/`canceled`/`duplicate`, counting `done` only, `percent` unrounded or `null` at zero eligible (151-166). Same contract used by list/get/resolve/tickets (`get/list/resolve-project.ts` modified).
- OpenAPI: `progress: projectProgressSchema` (`index.ts:27,51,81`) — `v.null_()` placeholder gone.

### 3. Events/invalidation absent → FIXED

- `project.updated` published post-commit from add (`add-project-ticket.ts:99`) and remove; `publish-project-ticket-updates.ts` wired into all seven task mutation boundaries (update-status, set-archived, update, delete, restore, permanently-delete, move) via in-transaction membership lookup; cross-org move deletes membership atomically inside the move transaction (`move-task.ts:176-183`).
- `project-sync-invalidation.ts` extended with `['project-tickets', projectId]` + bare-prefix fallback (lines 22-24); mutation hooks invalidate `['projects']`/`['project', id]`/`['project-tickets', id]` through it.

### 4. Web vertical absent → FIXED with gaps (declared)

- Present: `project-tabs/-tickets/-ticket-row/-ticket-picker.tsx`, `$projectSlug/tickets.tsx` route (alias-replace navigate correct), fetchers ×3, query + mutation hooks, Overview/Row progress consumption, i18n keys in `en-US.json` and `schema.json` in lockstep (`ticketRequired`/`ticketAlreadyScoped`/`ticketMustBelongToOrganization`/`alreadyScoped`/`noEditableTickets`/tabs/progress keys verified in both).
- Gap A: `routeTree.gen.ts` not regenerated — grep for `projectSlug/tickets` in the generated tree: **0 hits**; the Tickets route is unreachable in a build made from the current worktree. I proved the sanctioned fix works: `cd apps/web && pnpm exec vite build` (1m41s, success) regenerated the tree with `LayoutAuthenticated…ProjectsProjectSlugTicketsRoute` (routeTree lines 82/554-558); I then reverted my regeneration — QC does not implement. S4's mandatory `pnpm build` produces this file; **S4 condition 1: commit must contain the regenerated routeTree**.
- Gap B: picker is a taskId input, not an "only editable-tickets" filtered list. Server-side authorization is fully enforced (add controller Board-`edit` check → 404), so this is UX/spec polish, not a security hole. Carried as known gap.
- Gap C: component case 9 (`$projectSlug/tickets.test.tsx` alias replacement) not written. Behavior implemented in both route files; untested. Carried as known gap.

### 5. RED-first unexecuted → FIXED in substance

- `tests/api-integration/project-ticket.test.ts` exists at the spec path, 786 lines, all ten spec cases (titles verbatim from spec §RED-first plan).
- Component suites: 8 spec cases implemented (21 tests; case 9 absent — Gap C).
- RED evidence: stage JSON records all 10 integration cases failing pre-implementation (routes absent → 404 + missing relation). No verbatim retained artifact — minor process shortfall; the cycle-1 blocking issue (no tests at all + suite blind to the constraint) is resolved.

### 6. model-roster.md undeclared edit → NOT REVERTED, content now orchestrator-consistent

Diff still present, but now records the cycle-2 implementer swap (`ocg/deepseek-v4-pro` in, `glm/glm-5.3` demoted reviewer-only) — i.e., orchestrator-owned state that happens to live in the worktree. **S4 condition 2: orchestrator acknowledges/owns this line in the merge commit.** Minor.

## Test runs (executed by QC, 2026-08-27)

| Command | Result |
|---|---|
| `cd apps/api && DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kaneo_test pnpm exec vitest run --config vitest.integration.config.ts ../../tests/api-integration/project-ticket.test.ts` | **PASS — 10/10** (105.2s, real Postgres, mounted API) |
| `cd apps/api && pnpm exec vitest run --config vitest.config.ts ../../tests/api/project/project-ticket.test.ts` | **PASS — 2/2** (incl. `pins the unique task_id constraint`) |
| `cd apps/web && pnpm exec vitest run --config vitest.config.ts src/components/project/ src/lib/project-sync-invalidation.test.ts` | **PASS — 9 files, 21/21** |
| Same unit suite with `unique("project_ticket_task_unique")` removed from `schema.ts` | **RED — `pins the unique task_id constraint` FAILED** (negative control now binds; cycle-1's blind-suite defect is fixed) |
| After restore | PASS 2/2; `git diff` back to baseline (+24, no deletions) |
| Sabotaged-schema integration spot-check (`-t "rejects a second Project atomically"`) | PASS — 409 still produced by the in-transaction pre-check (`add-project-ticket.ts:76-85`); DB unique is the backstop, pinned by the unit test above. Acceptable layering. |
| `cd apps/web && pnpm exec vite build` | PASS (1m41s) — regenerates routeTree with the tickets route; reverted by QC |

Environment note (not a defect): the repo `.env` points at a remote dev Postgres (`192.168.3.100:5433`, role without CREATEDB) whose `kaneo_test` does not exist; a naive run fails all 10 tests at bootstrap with `permission denied to create database`. The provisioned local `kaneo_test` (localhost, 55 tables incl. `project_ticket`, hand-populated drizzle marker — the implementer's declared deviation) is the working target. S4 full-gate runs must set `DATABASE_URL` accordingly or provision a fresh `_test` DB.

## Scope audit (git status, branch `stellarc/kfl367-ticket-scope`, HEAD `cff72b9d`)

27 modified + 28 new paths, all within ticket-owned surfaces (`apps/api/src/{database,project,task}`, `apps/web/src`, `i18n`, `tests`, `drizzle`) plus declared shared files. Cross-checked against declared `shared_file_edits`: all 8 declared files are indeed modified and used. Task-controller edits (7 files) are the spec-required propagation boundaries — in scope per spec §Event and cache cutover, though not itemized in `shared_file_edits`; noted, not faulted (the JSON `summary` describes them). No worker debris outside `/tmp`; no peer worktree paths swept in (KFL-368/370 pipeline files are orchestrator dispatch artifacts, untracked). Cosmetic: journal 0072 entry re-indented (tabs→spaces) — churn only, `idx/when/tag` semantics unchanged. QC side effects (schema sabotage ×2, routeTree regen, dist/) fully reverted; worktree diff is byte-identical to pre-review state.

## Residual gaps carried to S4 (none block PASS)

1. **S4 must ship a regenerated `routeTree.gen.ts`** (falls out of its mandatory `pnpm build`; verified regenerable).
2. Picker board-edit-filtered candidate list (server already enforces; UX polish).
3. Component case 9 route-alias test absent.
4. Verbatim RED output not retained (summary only).
5. `model-roster.md` line ownership at merge.

**Stage decision: PASS → S4 merge-check.**
