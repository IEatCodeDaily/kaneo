# KFL-378 QC Cycle 2 — PASS

## Verdict

**PASS**

The prior API-runner, route-denial, and nested-list defects are fixed in the actual working-tree diff.

## Verified behavior

- The API Vitest config includes both `../../tests/api/**/*.test.ts` and `src/**/*.test.ts`.
- The normal API runner collected both required files. Full API run: **107 files passed, 557 tests passed**.
- Focused API run: **2 files passed, 6 tests passed**:
  - `src/project/controllers/list-project-sidebar.test.ts`
  - `../../tests/api/project/project-sidebar-permission.test.ts`
- The route test imports the real Project router and real `requireOrganizationPermission` guard. It allows admin, member, and viewer; rejects a non-member and a scope-limited service key with 403; and asserts projection is not reached on denial.
- The controller remains fixed-query/batched: one requester-filtered Project load, three batched relationship queries, three batched resource authorization checks, and at most three batched summary queries. Inaccessible IDs are filtered before safe-summary projection; empty Projects remain present.
- `NavProjects` now renders every `ul` with direct `li` children and no `li > li`. Team and Docs are informational, non-button rows.
- Disclosure state toggles correctly, persists per organization, hydrates only after the organization key exists, and auto-expands the active Project without expanding every Project.
- Canonical routes remain correct for Project overview, Board, Repo code, and Table.
- Focused UI run: **1 file passed, 6 tests passed**.
- Biome on the ten changed production/test files: **clean** (`Checked 10 files ... No fixes applied`).

## Guard-deletion negative control

Performed a reversible semantic sabotage by deleting only the sidebar route's `requireOrganizationPermission({ project: ["read"] })` middleware.

Expected red result:

- Route suite exit: **1**
- **2 failed, 3 passed**
- Scope-limited service key: expected 403, received 200
- Non-member: expected 403, received 200

The source file was restored byte-for-byte (`RESTORED True`). This proves the denial assertions depend on the real guard and fail for the intended semantic reason.

## Scope hygiene

QC did not edit production code, tests, or `tests/e2e`. QC writes are limited to:

- `.wayfinder/pipeline/KFL-378.qc.md`
- `.wayfinder/pipeline/KFL-378.json`
