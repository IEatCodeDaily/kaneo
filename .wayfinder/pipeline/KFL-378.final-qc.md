# KFL-378 Final QC — commit 3d3eee51 (post-rework)

**Date:** 2026-09-04 · **Mode:** read/test only; sole write this file · **Budget:** 15m

## Verdict

**PASS**

## Static gates

| Gate | Result |
|---|---|
| Biome, exact changed files (28 TS/JSON of the commit) | `Checked 28 files in 195ms. No fixes applied.` |
| Focused web tests (6 files: app-sidebar, nav-main, nav-projects, user-avatar, members-route, features) | **6 files / 16 tests passed** |
| Focused API tests (list-project-sidebar, organization-work-feature, project-sidebar-permission) | **3 files / 8 tests passed** |
| `pnpm build` (apps/web) | ✓ built in 1m 27s (chunk-size advisories only) |

## Content review

- **Migration journal minimal:** `0078_work_feature_flag.sql` is one line — `ALTER TABLE "organization" ADD COLUMN "work_enabled" boolean DEFAULT false NOT NULL;` — with a single appended `_journal.json` entry (idx 78, tag 0078). No extra DDL, no snapshot churn beyond the generated one.
- **Settings single bottom nav button:** `app-sidebar.tsx:112-123` — one `aria-label="Settings"` button in `SidebarFooter` navigating to `/dashboard/settings/account/information`; `getAllByRole("button", { name: "Settings" })` has length 1 (test green). Not a disclosure section.
- **Work flag default false + containment:** schema line 188 `workEnabled: boolean("work_enabled").default(false).notNull()`; Better Auth additionalField `defaultValue: false` (auth.ts:348-353). Containment verified on both branches of the render gate (`app-sidebar.tsx:89-110`): flag off → no Work pill, no `NavWork`, Resources render directly; flag on → pill selects non-persistent `work|resources` mode. Test `contains Work completely…` asserts null Work button/nav-work plus present boards/repos/tables. The sidebar API call is only reachable through `NavProjects` inside `NavWork`, which only mounts when `workEnabled`.
- **Recent max5:** `rememberRecentPage` dedupes by pathname and `.slice(0, 5)` (user-preferences.ts:210-218), persisted via zustand `persist` to localStorage — real persisted state, not a fixture. Render (nav-main.tsx:96-111) maps up to 5 items; indices ≥3 get `hidden min-[900px]:block` → 3 ordinary, 2 tall via CSS. Disclosure is `recentOpen` state, and the whole Recent row renders only when `recentPages.length > 0` — no fake empty disclosure. Only renderer of `recentPages` in the app.
- **No inert Initiatives/Digest/Views:** `grep` over `apps/web/src` for `Initiatives|Digest|Views` → no matches. `NavWork` renders only the collapsible Projects section. (Within an expanded Project, inert `Boards`/`Cycles` rows are spec'd placeholders with `aria-disabled="true"` — different items, per spec.)
- **Projects tree:** independent per-project chevron disclosure; Resources sub-disclosure expands into Boards/Repos/Tables type rows (each with its own toggle, count in a separate `w-5` span, chevron in a separate span) then canonical linked resources navigating the real board/repo/table routes. Disclosure persisted per org key `kaneo:project-sidebar:<orgId>`, hydrated only after org id exists, active project auto-expanded without expanding siblings. Tests cover all of the above including `li > li` structural validity.
- **Ctrl+K members route slug:** `getOrganizationMembersRoute(organizationSlug)` returns `to: "/dashboard/organization/$organizationSlug/members", params: { organizationSlug }` (members-route.ts), consumed by the palette (index.tsx:134). Zero references to the nonexistent `/dashboard/settings/organization/members` path.

## Negative control

First attempt — adding `hidden` to the project chevron's Tailwind class — left the suite **green**: the suite mocks UI primitives, so Tailwind classes are inert in jsdom. Correctly identified as a weak probe, discarded; file restored byte-identical before proceeding.

Valid control — behavioral sabotage on `apps/web/src/components/nav-projects.tsx`: replaced the project disclosure `onClick={() => toggle(project.id)}` with a no-op attribute.

- Expected red: **1 file failed — 3 failed / 3 passed** (disclosure, persistence, and active-project tests all red).
- Restored from backup: md5 `cfc287e5829fe9066671125ff6288fdd` on both source and backup — **byte-identical**.
- Post-restore: suite green again (**6/6 passed**). Backup removed; `git status` shows zero changes under apps/packages/i18n/tests.

The disclosure tests genuinely depend on the real toggle handler.

## Scope hygiene

QC touched no production, test, or e2e file (verified via `git status` after restore). Sole write: `.wayfinder/pipeline/KFL-378.final-qc.md`.

Pre-existing repo-wide lint debt outside this commit's file set remains, as documented in the commit message.
