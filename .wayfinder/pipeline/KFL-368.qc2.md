# KFL-368 S3 QC cycle-2 verdict — REWORK

## Verdict

**REWORK.** Defect 2 remains: migration `0073` uses the wrong PostgreSQL exception condition for its two retry guards. The required focused integration run could not start because this worktree has no installed `vitest` executable; that does not override the independently reproduced migration defect.

## Cycle-1 defect verification

| Defect | Result | Evidence |
|---|---|---|
| 1. Idempotent unlink | **PASS (source + coverage)** | `delete-project-resource-link.ts:41–43` now returns on absent association. Integration tests at `project-resource-links.test.ts:502–568` require two 204 deletes for Board, Repo, and Table. |
| 2. Migration retry safety and constraints | **FAIL** | `0073_project_resource_links.sql:49–59` catches `duplicate_object` for `ALTER TABLE … ADD CONSTRAINT … UNIQUE`. On PostgreSQL, re-adding an existing named constraint raises SQLSTATE `42P07` (`duplicate_table`), not `duplicate_object`: QC reproduced that exact statement in `kaneo_migration_probe`; the `duplicate_object` handler did not catch it and the block failed. Thus retry/partial-apply safety remains broken. The migration correctly uses composite project/resource FKs and test source covers API cross-org Board/Repo/Table rejection at `:194–230`, but that cannot cure the migration guard defect. |
| 4. Named coverage | **PARTIAL** | Added repeat-delete cases cover all three types. Existing tests retain cross-org API rejection, hidden Board link omission, no-grant link omission, no-progress, Board cascade, and event paths. They still do not prove direct composite-FK rejection for all three tables, all-table cascades, Project archive preservation, or Repo/Table hidden-target/canonical-access variants required by the spec. |
| 5. Unrelated roster file | **PASS** | `git status --short` contains no `.wayfinder/pipeline/model-roster.md`; changed pipeline artifacts are KFL-367/368/370 handoff state, not a KFL-368 roster edit. |

## Focused test execution

Attempted from the ticket worktree with the mandated fnm PATH:

```text
pnpm exec vitest run --config apps/api/vitest.integration.config.ts tests/api-integration/project-resource-links.test.ts
ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "vitest" not found
```

The worktree has no `node_modules/.bin/vitest`; therefore QC could not independently execute the claimed 17/17 focused suite. This is recorded as unavailable evidence, not as the primary failure.

## Sabotage negative control

**PASS.** On disposable database `kaneo_migration_probe`, a sabotaged `CHECK (relationship IN ('context'))` rejected insert of `dependency` with a check-violation error (RED). Dropping it and restoring `CHECK (relationship IN ('context','dependency','deliverable'))` accepted the same insert (GREEN). This validates the control mechanics and the relationship-check requirement, but not migration retry safety.

## Scope audit

Ticket implementation changes are within resource-link API/web, migration, project router/overview/invalidation touchpoints, i18n, and focused integration coverage. The worktree also contains KFL-367 and KFL-370 pipeline artifacts; they are pre-existing shared-worktree state and must not be delivered as KFL-368 changes.

## Required correction

Change both composite-unique guard exception clauses in `0073_project_resource_links.sql` (repo and data_table) to catch the actual PostgreSQL duplicate-constraint SQLSTATE/condition (`duplicate_table`, as reproduced), or use an explicit catalog/existence guard. Re-run the complete focused integration suite in an environment with dependencies installed, then re-QC.
