# KFL-367 QC review — REWORK (cycle 1/3)

Reviewer: `omp/9router/glm/glm-5.3` (different family from implementer `cx/gpt-5.6-luna`). Stage doc: `docs/agents-pipeline.md` §S3.

## Gate

`spec-review=pass`, `implementation=ready-for-qc` present in `.wayfinder/pipeline/KFL-367.json`. Review input: S1 spec + actual diff, not the implementer summary.

## Verdict

**REWORK.** The worktree contains only the Persistence slice (~61 lines: `project_ticket` table, relations, exports, migration `0073`, journal entry) plus one trivial export-existence unit test. Six of the ticket's named targets: one is implemented (partially tested), five are entirely absent. The spec's RED-first plan was not executed (0/10 integration cases, 0/9 component cases, 0 E2E). The implementer's own stage JSON admits this (`missing` lists membership controllers, projection, task propagation, web verticals; `pass_counts: "0 verified"`), and the diff confirms it.

## Diff audit (scope)

`git status` (branch `stellarc/kfl367-ticket-scope`, HEAD `cff72b9d`):

- Modified: `apps/api/src/database/schema.ts` (+24), `relations.ts` (+20), `index.ts` (+3), `apps/api/drizzle/meta/_journal.json`, `.wayfinder/pipeline/model-roster.md`
- New: `apps/api/drizzle/0073_project_ticket.sql`, `tests/api/project/project-ticket.test.ts`
- **Zero changes** under `apps/api/src/project/`, `apps/api/src/task/`, `apps/web/`, `tests/api-integration/`, `apps/e2e/` (verified by `git status` + repo-wide `projectTicket|project-tickets|project_ticket` grep: hits only in database files, migration, journal, and the one test).

Shared-file edits match the declared `shared_file_edits` (schema/relations/index) exactly — PASS on touchpoint discipline. One undeclared edit: `.wayfinder/pipeline/model-roster.md` (roster line for glm-5.3 demoted to reviewer-only) is orchestrator-owned tracker state, not declared by S2 and outside ticket-owned paths (`project membership/progress API+web`). Minor defect; revert or have the orchestrator own that line. Untracked `KFL-368/370` spec/json files in this worktree are orchestrator dispatch artifacts, not implementer debris — noted, not faulted.

## Named audit targets

### 1. Unique `task_id` constraint (one project per ticket) — PASS (artifact), UNDEFENDED by tests

- Schema: `schema.ts:714-736` — `unique("project_ticket_task_unique").on(table.taskId)` + defensive `unique("project_id","task_id")`, indexes `(project_id, rank)` and `(task_id)`, no `organization_id`, no `task.projectId`. Matches spec §`project_ticket` column/constraint table exactly.
- Live DB proof (real Postgres, inside `BEGIN;…ROLLBACK;`): seeded two projects in one org, inserted the same `task_id` into both → `ERROR: duplicate key value violates unique constraint "project_ticket_task_unique"`. Rolled back; no residue.
- **But no test binds it.** Sabotage negative control: removed `unique("project_ticket_task_unique")` from `schema.ts` → the only existing test stayed **GREEN** (1 passed). Reverted → GREEN unchanged. The suite is blind to the headline constraint; spec integration case 3 (`rejects a second Project atomically`, asserts 409 + first row unchanged) does not exist.

### 2. Same-org transaction proof in add controller — FAIL

No add controller exists. `apps/api/src/project/controllers/` contains only KFL-366 files (`archive/create/get/list/rename-slug/resolve/unarchive/update`). Repo-wide grep for `projectTicket` in `apps/api/src` hits only `database/{schema,relations,index}.ts`. `project.organizationId === task.board.organizationId` is proven nowhere.

### 3. 409/404 no-leak contract — FAIL

`apps/api/src/project/index.ts` is untouched: no `GET/POST /project/:id/tickets`, no `DELETE /project/:id/tickets/:taskId`, `progress: v.null_()` still at line 52. No 409-on-duplicate, no non-leaking 404, no idempotent-remove 404 path.

### 4. Progress eligibility taxonomy — FAIL

No derivation anywhere. `status-taxonomy.ts` is untouched (correct — frozen contract), but nothing computes `completed`/`eligible`/`percent`; `canceled`/`duplicate`/`archivedAt`/`deletedAt` exclusion is implemented in zero queries. Spec case 5 has no implementation or test.

### 5. Projection replacing `withPresentationOnlyFields` everywhere — FAIL

`project-projection.ts:44-48` still defines it; call sites at lines 71, 84, 101 (`findProjectById`, `findProjectBySlug`, `listProjectsForOrganization`) unchanged; OpenAPI contract still `progress: v.null_()` (`index.ts:52`). Every projection still returns the KFL-366 placeholder.

### 6. Membership mutations publishing project sync events — FAIL

No membership mutations exist; no `project.updated` publication; task-mutation boundaries (status/archive/delete/move) contain no `project_ticket` lookup (grep: no references); `apps/web/src/lib/project-sync-invalidation.ts` not extended (no `['project-tickets', …]` key anywhere in the repo).

## Demand checklist

| Demand | Result |
|---|---|
| (1) RED-first proof + sabotage negative control | **FAIL** — 0/10 integration cases at spec path `tests/api-integration/project-ticket.test.ts` (file absent); only `tests/api/project/project-ticket.test.ts` (13 lines, export-existence). Sabotage (removed `task_id` unique) stayed GREEN → no regression binding. Implementer's `red` field admits RED never ran. |
| (2) Focused suites run by QC | Done — see Test runs. The single new test passes; it proves only that the symbol exports. |
| (3) No scope creep outside ticket-owned paths | PASS with one defect — `model-roster.md` edit is undeclared, outside ticket-owned paths (orchestrator state). |
| (4) Shared-file edits ≤ declared touchpoints | PASS for code — `schema.ts`/`relations.ts`/`index.ts` only, as declared. |
| (5) i18n en-US + schema lockstep | **FAIL/N-A** — no `projects.tabs.*`, `progress.*`, `tickets.*`, `validation.*` keys added to either `apps/web/src/i18n/en-US.json` or `schema.json` (`grep '"tickets"'` → 0). Consistent with the absent web vertical, but required by spec. |
| (6) Migration 0073 defensive + journal-consistent | **PASS** — all statements `IF NOT EXISTS`; `idx:73/when:1787800000000/tag:0073_project_ticket` follows the hand-appended journal convention (0070/0071 are likewise round-numbered); no snapshot required (repo has none past `0041_snapshot.json`, incl. 0072). `drizzle-kit migrate` applied cleanly against real Postgres; live `\d project_ticket` matches spec DDL exactly (FK cascade/restrict, both uniques, both indexes); `__drizzle_migrations` row 110 recorded. Missing `statement-breakpoint` separators match 0070's precedent and the file applied as-is. |
| (7) Push events + invalidation wired | **FAIL** — nothing wired; see target 6. |

## Test runs (executed by QC)

| Command | Result |
|---|---|
| `cd apps/api && pnpm exec vitest run --config vitest.config.ts ../../tests/api/project/project-ticket.test.ts` | PASS — 1 file, 1 test |
| Same, with `project_ticket_task_unique` removed from `schema.ts` (sabotage) | **PASS — control FAILED to go RED** (suite blind to the constraint) |
| Same, after revert | PASS — 1 test; constraint line restored verbatim (`schema.ts:731`), diff back to +24 |
| `cd apps/api && pnpm exec vitest run --config vitest.config.ts ../../tests/api/database/` | PASS — 3 files, 15 tests (full schema/relations import graph loads) |
| `cd apps/api && pnpm exec drizzle-kit migrate` | PASS — 0073 applied; journal consistent |
| psql duplicate-`task_id` probe in rolled-back tx | Unique violation triggered as designed |

## Defects to fix (append to spec for S2 rework)

1. **Membership API absent.** Add `GET/POST /project/:id/tickets`, `DELETE /project/:id/tickets/:taskId` to `apps/api/src/project/index.ts` with `fromProject` + `project:read`/`project:update` + independent `fromTask`-equivalent task access (`task:update`, Board `edit`), controllers under `apps/api/src/project/controllers/`: same-org proof (`project.organizationId === task.board.organizationId`) inside the insert transaction, `409` on existing membership, non-leaking `404`, idempotent-remove `404` that never writes Task fields. Spec cases 1-4, 7.
2. **Progress projection absent.** Replace `withPresentationOnlyFields` (project-projection.ts:44-48, call sites 71/84/101; OpenAPI `progress: v.null_()` at index.ts:52) with the shared set-based, authorization-filtered projection: visible-Board join, exclude `deletedAt`/`archivedAt`/`canceled`/`duplicate`, count `done` only, `percent` unrounded or `null` at `eligible === 0`. Same contract for list/get/resolve/tickets. Spec cases 5, 6, 8.
3. **Events/invalidation absent.** Publish `project.updated` (`organizationId`+`projectId`, post-commit, no ticket payload) from add/remove and from task status/archive/delete/move boundaries via `project_ticket` lookup in-transaction; cross-org task move removes membership atomically. Extend `lib/project-sync-invalidation.ts` with `['project-tickets', projectId]`. Spec cases 9, 10.
4. **Web vertical absent.** Tickets tab/route (`$projectSlug/tickets.tsx`, regenerated `routeTree.gen.ts`), `project-tabs`, `project-tickets`, `project-ticket-row`, `project-ticket-picker`, Overview/Row/List/ProjectsOverview progress consumption, fetchers + query/mutation hooks (`['project', id]`, `['project-tickets', id]`, `['projects']` invalidation), i18n en-US+schema keys in lockstep. Spec component cases 1-9.
5. **RED-first plan unexecuted.** Create `tests/api-integration/project-ticket.test.ts` with the ten spec cases (real Postgres, mounted API — no controller-only substitutes), run RED, retain verbatim output; add the nine component tests; re-capture the negative control against the real suite. The current export-existence test must not survive as the only coverage; it must at minimum pin the `task_id` unique so the sabotage control goes RED.
6. **Undeclared tracker-state edit.** `.wayfinder/pipeline/model-roster.md` — revert from the implementer worktree or re-attribute to the orchestrator; S2 declared only the three database files.

## Positive notes

- The Persistence slice that does exist is spec-exact: column types/defaults, FK actions (`cascade` project/task, `restrict` user delete), both unique constraints, both indexes, relations on both sides, exports. Migration 0073 is defensive and journal-consistent, proven by live apply.
- No regression risk elsewhere: nothing outside the three declared database files and the new test/migration was touched; KFL-366 guards (`tests/api/legacy-project-guard.test.ts` untouched, `apps/api/src/project/` untouched) remain intact.

**Stage decision: REWORK → S2, defects above appended.**
