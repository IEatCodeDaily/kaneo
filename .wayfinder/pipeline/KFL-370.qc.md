# KFL-370 QC review — REWORK

## Gate

`spec-review=pass` and `implementation=ready-for-qc` cycle 2 were present before review. This retry found no predecessor QC verdict artifact.

## Verdict

**REWORK.** The API slice is incomplete and not release-verifiable; the required Web Updates product surface is absent; `i18n/schema.json` is invalid JSON; migration 0073 does not defensively install its health constraint and has no generated snapshot; and the focused integration suite currently fails 13/13 before test bodies because the harness cannot create its database.

## Prioritized cycle-3 defect list

1. **P0 — Implement the specified Web Updates surface end to end.** Missing: Updates route, panel, list, row, composer, edit dialog, delete dialog, Overview latest-health integration, Project row/list latest-health integration, and all nine specified component scenarios. Only the badge, staleness indicator, fetchers, hooks, invalidation, and websocket primitives exist. This admission is confirmed by the scope audit: no route or six principal Update component files exist. Latest-update-wins is therefore not shipped to users despite the API list ordering correctly deriving it from newest rows.
2. **P0 — Repair and validate `i18n/schema.json`.** `python3 -c 'import json; json.load(open("i18n/schema.json"))'` fails at line 13088 column 17 with `JSONDecodeError: Expecting property name enclosed in double quotes`. The immediate defect is a trailing comma after the `labels.required` array/object boundary. Validate the repaired schema against `i18n/en-US.json`; the current invalid schema blocks locale/schema tooling and build gates.
3. **P0 — Make migration 0073 genuinely defensive and add its generated snapshot.** `CREATE TABLE IF NOT EXISTS` contains the health CHECK inline. If `project_update` already exists without that constraint, rerunning 0073 silently leaves the sabotage control absent. Add the CHECK independently using the specified `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN null; END $$;` pattern. Also generate the required `apps/api/drizzle/meta/0073_snapshot.json`; only the journal entry exists. Define the latest-update index with `created_at DESC` as specified; current SQL and Drizzle declaration omit descending order.
4. **P0 — Restore a runnable integration environment and get the exact suite green.** Current result is 13/13 failed before test bodies: `resetTestDatabase()` calls `ensureTestDatabaseExists()`, and PostgreSQL returns `error: permission denied to create database`. This run provides no behavioral evidence for health enum enforcement, author/membership guards, hard delete, latest-update-wins, archive behavior, or events. Supply an existing migrated test database or CREATE DATABASE privilege, then rerun the exact command and fix all product failures.
5. **P1 — Correct the integration suite to enforce the specification rather than documenting deviations.** The cross-org test expects 403 and the invalid Project ID test expects 400, while the spec requires identical `404 Project not found` for cross-organization, cross-project, missing, and invalid IDs. The progress/health sabotage control mutates `taskTable` directly, despite requiring mutation through the public Board API. The push test observes domain events only; it does not register/assert `registerProjectSyncBroadcast` forwarding `PROJECT_UPDATE_*` to an org-member socket. These tests can pass while three named contracts remain broken.
6. **P1 — Add explicit latest-health behavior coverage.** The list projection orders by `created_at DESC, id DESC` and delete hard-removes rows, which is the intended derived/no-stored-health design. However, no runnable proof currently establishes equal-timestamp ordering, next-newest health after deletion, or zero-row `No update`; the Web consumers that would display those states are absent. Preserve the design: no Project health column, no `no-update` row, no progress-derived mutation.
7. **P1 — Run required quality gates after implementation.** Biome was admitted as not run. It will currently flag at least unused `publishEvent` imports in `list-project-updates.ts` and `update-guards.ts`, and the new badge/staleness files are compressed one-line implementations inconsistent with repository formatting. Run locale validation, focused API/component tests, `pnpm exec biome ci .`, and `pnpm build` after the P0 work.
8. **P2 — Remove unrelated pipeline changes from ticket scope.** `.wayfinder/pipeline/model-roster.md` is modified and KFL-367/KFL-368 pipeline artifacts are untracked in this worktree. They are not KFL-370 implementation deliverables. Keep cycle-3 changes limited to KFL-370 plus inherited predecessor work.

## Named target audit

- **Migration 0073 defensive:** **FAIL.** FK additions are duplicate-safe and table/index creation is idempotent, but the health CHECK is not independently installed on a pre-existing table; generated snapshot absent; composite index lacks explicit descending order.
- **Health enum enforced / sabotage control:** **PARTIAL, unverified at runtime.** Valibot uses the exact three-value picklist and the Drizzle schema declares the same SQL CHECK. The migration defect can omit the DB control on a pre-existing table, and the integration suite never reached its assertions.
- **Latest-update-wins, no stored health:** **API design PASS; shipped behavior INCOMPLETE.** `project_update` owns authored health, list ordering selects newest first with stable ID tie-break, hard delete exposes the next row, and no Project health/staleness column or `no-update` enum value was found. Required Web derivation/display is absent.
- **Scope:** No notifications, followers, initiative updates, health-history table, stored staleness, or progress-derived health path found. Unrelated pipeline artifacts remain in the worktree as noted above.

## Required integration run — verbatim failure

Command:

```text
cd apps/api && pnpm exec vitest run --config vitest.integration.config.ts ../../tests/api-integration/project-update.test.ts
```

Result:

```text
❯ ../../tests/api-integration/project-update.test.ts (13 tests | 13 failed) 301ms
     × creates an organization-scoped Project Update with health on-track 49ms
     × rejects health values outside on-track|at-risk|off-track with no row written 22ms
     × rejects empty or whitespace-only or oversized content 17ms
     × lists updates newest first and returns [] for a Project with zero updates 17ms
     × edit appends a pre-edit snapshot to edit_history without compression 22ms
     × edit by a non-author returns the identical 404 used for missing rows 20ms
     × edit by a former organization member returns 404 even though author_id still resolves 21ms
     × delete is a hard delete and the next-newest Update becomes the latest health 19ms
     × cross-organization and cross-project access return identical 404s 26ms
     × progress changes never mutate authored health 16ms
     × push events fire with the contract { organizationId, projectId, updateId, health } 33ms
     × view-privilege Project grants can list but not create 16ms
     × archive does not invalidate the Update history surface (rows survive archive) 19ms

error: permission denied to create database
 ❯ ../../node_modules/.pnpm/pg@8.22.0/node_modules/pg/lib/client.js:652:17
 ❯ ensureTestDatabaseExists ../../tests/api-integration/helpers/database.ts:55:7
     53|
     54|     if (result.rowCount === 0) {
     55|       await adminClient.query(
       |       ^
     56|         `CREATE DATABASE ${quoteIdentifier(databaseName)}`,
     57|       );
 ❯ ../../tests/api-integration/helpers/database.ts:67:7
 ❯ ensureTestDatabaseMigrated ../../tests/api-integration/helpers/database.ts:75:5
 ❯ resetTestDatabase ../../tests/api-integration/helpers/database.ts:83:3
 ❯ ../../tests/api-integration/project-update.test.ts:54:5

Test Files  1 failed (1)
Tests       13 failed (13)
Duration    10.32s
```

## Additional test observation

Attempting the helper test from repository root failed because that workspace has no `vitest` binary. Attempting it under `apps/api` with the API config reported no matching file because that config includes only `../../tests/api/**/*.test.ts`; the test path is valid for that include but the initial command named the wrong relative path. Cycle 3 should run it as `cd apps/api && pnpm exec vitest run --config vitest.config.ts ../../tests/api/project/append-update-edit.test.ts`.

Verdict: **REWORK**.
