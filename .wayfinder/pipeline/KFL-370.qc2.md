# KFL-370 QC review — REWORK

## Cycle 4 verdict

**REWORK.** API integration and no-leak authorization behavior are verified. The delivered web surface is not acceptance-verifiable because no focused component tests implement the nine required scenarios, and the changed-path Biome gate reports new errors.

## Per-target evidence

1. **Project Update integration — PASS.**
   ```text
   pnpm exec vitest run --config vitest.integration.config.ts ../../tests/api-integration/project-update.test.ts
   Test Files  1 passed (1)
   Tests  14 passed (14)
   ```

2. **Organization-access middleware collateral — PASS.**
   The 403-to-404 conversion is narrowly guarded by a `lookup` source whose resource is `project`; the unresolved-id path uses the same predicate. Non-project routes retain the ordinary 400 unresolved-organization and propagated authorization behavior. Regression runs passed:
   ```text
   project.test.ts: 9 passed
   board.test.ts:   3 passed
   ```

3. **Sabotage control — PASS.**
   Copied the middleware to `/tmp/KFL-370.organization-access-middleware.ts`, changed only the cross-organization project-route mapping from `404 Project not found` to `403 Forbidden`, then ran the single cross-organization test. It failed as required at `project-update.test.ts:409`: expected 404, received 403. Restored the exact scratch copy, reran the same test: 1 passed / 13 skipped. Deleted the `/tmp` copy.

4. **Web Updates component scenarios — FAIL.**
   Repository test discovery found no focused component suite for the new Updates components; only `project-update.test.ts` integration coverage exists. Therefore none of the nine required web scenarios are proven by focused component tests.

5. **Locale and Biome — FAIL.**
   `i18n/schema.json` parses as valid JSON. The simple top-level key-set equality probe is not the schema/en-US lockstep contract because `schema.json` is JSON Schema rather than a locale tree; no appropriate lockstep validator was supplied/run. The changed-path `pnpm exec biome ci ...` failed with 8 errors and 4 warnings, including unused `projectUpdateTableRelations`, unused `publishEvent` imports in two new controllers, unused `invalidCuid`, unsorted relations imports, and formatting violations. These are gate failures in changed files, not an inherited-baseline result.

6. **Scope — PARTIAL.**
   `git status --short` reports 44 staged, 3 unstaged, and 4 untracked paths. The shown implementation paths are KFL-370 project-update work plus declared shared touchpoints including `organization-access-middleware.ts`; final scope admission remains blocked until the unrelated/stale worktree entries are identified or removed from the ticket change set.

## Defects

1. Add focused web component tests covering all nine specified Updates scenarios and make them pass.
2. Fix the changed-file Biome errors/warnings and rerun the changed-path gate cleanly.
3. Demonstrate `i18n/schema.json` ↔ `i18n/en-US.json` lockstep using the repository's actual schema-validation mechanism.
4. Resolve or explicitly classify every non-KFL-370 worktree path before final scope approval.
