# KFL-368 S3 QC verdict — REWORK

## Verdict

**REWORK.** Do not advance. The implementation has working positive-path evidence, but misses a named behavioral contract and does not meet the required QC proof/coverage standard.

## Per-target verdicts

| Target | Verdict | Evidence |
|---|---|---|
| Three dedicated type-safe link tables with FK integrity; no polymorphic table | **FAIL** | `0073_project_resource_links.sql` defines the three required tables and composite FKs, but migration re-run defensiveness is broken: `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE` is guarded with `WHEN duplicate_table THEN null` at lines 49–59. PostgreSQL raises `duplicate_object` for an already-existing constraint, so the migration fails on a partially applied/retried database. The integration test only exercises Board invalid relationship/rank; it does not prove mismatched-org composite FK rejection or all three tables. |
| Links grant no access; no hidden-resource metadata leak | **PASS (targeted)** | `list-project-resources.ts` calls `listAccessibleResourceIds` per type before building summaries; controller create/update/delete use `requireResourcePrivilege`. Focused integration subset passed: Board creation, no-grant link omission, and `project.updated` (3/3). Full suite cannot be accepted as proof; see test gate. |
| No progress impact | **PASS (static + implemented test)** | Project detail contract remains `progress: null` in `apps/api/src/project/index.ts`; integration case asserts null after a Board link. |
| Relationship enum `context|dependency|deliverable` | **PASS** | SQL checks on each table; shared API projection exports the typed relationship picklist. Focused database-constraint test passed (1/1). |
| Unlink idempotency | **FAIL** | Delete controller explicitly returns 404 when no association is found (`delete-project-resource-link.ts:41–43`). A second DELETE is therefore not idempotent. Its only unlink test executes one successful Board deletion; Repo/Table and repeated deletion are untested. |
| Project Overview integration preserves KFL-366 | **PASS (web regression)** | `ProjectContextualResources` is mounted in `project-overview.tsx` separately from scoped work. Web focused invocation completed **205 files / 1215 tests passed**, including Project overview and contextual-resource tests. |
| i18n en-US + schema lockstep | **PASS (structural)** | Both files contain the declared `projects.resources` hierarchy. Repository-wide `pnpm i18n:check` fails on pre-existing missing translations across many locales, not an en-US/schema mismatch. |
| Push events + invalidation | **PASS** | Create/update/delete publish `project.updated`; existing broadcast maps it to `PROJECT_UPDATED`; WebSocket calls `invalidateProjectQueries`, now including `['project-resources', projectId]`. Focused event assertion passed. |
| Scope and shared touchpoints | **PASS with handoff seam noted** | Diff is confined to resource-link API/web, migration/i18n, and declared Project router/overview/invalidation touchpoints. `.wayfinder/pipeline/model-roster.md` is an unrelated changed file and must not be included in KFL-368. No duplicate predecessor implementation was found. |

## Required RED-first control

**FAIL.** QC cannot record the demanded RED→GREEN sabotage proof. A temporary `/tmp` copy of 0073 was altered to restrict `project_board_relationship_check` to `context`; the proposed violation was confirmed by `diff`, then discarded. It was not executable against a disposable migrated PostgreSQL database because no `DATABASE_URL` is supplied. This is not a RED test result and does not satisfy the gate. Implementer must capture a real test-run RED for the sabotage, restore the source, and capture GREEN.

## Focused execution

- `pnpm --filter @kaneo/api test:integration -- tests/api-integration/project-resource-links.test.ts` — **FAIL**, because Vitest ran the complete integration suite rather than only this file; 24 failures are unrelated stale/infrastructure failures. The KFL-368 file itself was not reported failed.
- Direct named subset: `pnpm exec vitest run --config vitest.integration.config.ts ../../tests/api-integration/project-resource-links.test.ts -t 'links one same-organization Board|does not grant access through a Project Resource link|publishes project.updated'` — **PASS**, 3 passed, 12 skipped.
- Direct constraint target — **PASS**, 1 passed, 14 skipped.
- Web focused command — **PASS**, 205 files / 1215 tests.
- API bundle — **PASS**. Web typecheck fails on numerous unrelated pre-existing API/web errors; none are in KFL-368 files.

## Concrete defects

1. **Make unlink idempotent.** Repeated DELETE of a Project-scoped link must return the documented successful no-content result without mutating a Resource or emitting a duplicate event. Add Board/Repo/Table repeat-delete tests.
2. **Fix migration 0073 retry safety.** Change the two `ADD CONSTRAINT ... UNIQUE` guards from `duplicate_table` to the correct duplicate-constraint exception (or use an existence guard). Prove retry/partial-apply behavior and prove all three composite FKs reject cross-org pairs.
3. **Provide actual RED→GREEN evidence.** Run the sabotage against a disposable migrated database, capture RED, restore, then capture GREEN. A temporary text diff is insufficient.
4. **Complete named coverage.** Add omitted Project-hidden/Repo/Table discoverability cases, canonical target 404/list-omission proof, Repo/Table unlink preservation, project deletion/archive preservation, all-table cascade constraints, component dialog/row/unlink/mobile tests, and PROJECT_UPDATED cache invalidation path test.
5. **Exclude unrelated `.wayfinder/pipeline/model-roster.md` from ticket delivery.**
